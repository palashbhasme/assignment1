import 'package:flutter/material.dart';

class GlobalVariables{
  static const appbarColor = Colors.black;
  static const textColor = Colors.white;
  static const floatActionButtonColor = Colors.amberAccent;
}